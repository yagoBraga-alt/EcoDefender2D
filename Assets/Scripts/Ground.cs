using UnityEngine;

// Simple marker component. TrashItem checks for this to know it has landed.
// Avoids relying on Unity Tags (which need manual setup in Project Settings).
public class Ground : MonoBehaviour { }
